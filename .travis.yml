language: node_js
node_js:
  - "8"
install:
  - npm install
jobs:
  include:
    - stage: Test
      script: npm test
    - stage: Coverage
      script: npm install -g codecov && npm run coverage:test && npm run coverage:report && codecov
